spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        enabled: false
        dataId: gateway
        group: DEFAULT_GROUP
  application:
    name: gateway
