spring:
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        # false为关闭自动刷新,默认是true
        enabled: true
        #namespace: DEFAULT_GROUP
        shared-dataids: db.yml
        refreshable-dataids: db.yml
  application:
    name: provider
